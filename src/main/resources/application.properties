# database init, supports mysql too
database=h2
spring.sql.init.schema-locations=classpath*:db/${database}/schema.sql
spring.sql.init.data-locations=classpath*:db/${database}/data.sql

# Web
spring.thymeleaf.mode=HTML

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategySnakeCaseImpl
spring.jpa.properties.hibernate.default_batch_fetch_size=16

# Internationalization
spring.messages.basename=messages/messages

# Actuator
# Expose all actuator endpoints for monitoring and management purposes
# Don't do this in production, only for development and testing
management.endpoints.web.exposure.include=*

# Logging
logging.level.org.springframework=INFO
# logging.level.org.springframework.web=DEBUG
# logging.level.org.springframework.context.annotation=TRACE

# Maximum time static resources should be cached
spring.web.resources.cache.cachecontrol.max-age=12h

# OpenTelemetry: export traces and metrics to an OTLP endpoint (localhost:4318 by default).
otel.service.name=spring-petclinic
otel.exporter.otlp.protocol=http/protobuf
otel.traces.exporter=otlp
otel.metrics.exporter=otlp
otel.logs.exporter=none
otel.propagators=tracecontext
otel.traces.sampler=parentbased_traceidratio
otel.traces.sampler.arg=0.1

# Keep request and database telemetry useful, bounded, and free of form/header values.
otel.instrumentation.common.db.query-sanitization.enabled=true
otel.instrumentation.logback-appender.enabled=false
otel.instrumentation.http.server.exclude-pattern=/actuator(?:/.*)?|/webjars/.*|/resources/.*|/favicon.*
